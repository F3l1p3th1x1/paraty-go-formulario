{
  "version": 2,
  "framework": null,
  "installCommand": "npm install --omit=dev --omit=optional",
  "buildCommand": "",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
